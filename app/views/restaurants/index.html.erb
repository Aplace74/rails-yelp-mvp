<h1>Restaurants</h1><span><%= link_to 'add a restaurant', new_restaurant_path %></span>

<% @restaurants.each do |resto| %>
  <ul>
    <li>
     <%= link_to resto.name, restaurant_path(resto) %>
    </li>
  </ul>
<% end %>